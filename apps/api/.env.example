# Boost API Environment Variables

# Firebase Admin SDK
# For local development, download service account JSON from Firebase Console
# and set the path here. For Cloud Run, use the default credentials.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# CORS Origins (comma-separated)
# Add your frontend domains here
CORS_ORIGINS=http://localhost:3000,https://boost-dev-3fabf.web.app

# Environment
# Set to "production" in deployed environments
ENVIRONMENT=development
