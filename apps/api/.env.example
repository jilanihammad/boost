# ============================================================
# Boost API - Environment Variables
# ============================================================
# Copy to .env and fill in values.

# --- Environment ---
# "development" or "production". Controls CORS strictness, etc.
ENVIRONMENT=development

# --- Firebase / Google Cloud ---
# Option A: Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Option B: Inline JSON (useful for Cloud Run / Docker)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# --- CORS ---
# Comma-separated list of allowed origins.
# REQUIRED in production (ENVIRONMENT=production). Falls back to localhost in dev.
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# --- Sentry Error Monitoring ---
# Optional. If set, Sentry SDK is initialized for error tracking.
# Leave blank or unset to disable in development.
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# --- Boost App ---
# Base URL for QR code / token redemption links.
# Defaults to Firebase Hosting dev URL if not set.
# TODO: Set to real custom domain for production.
# BOOST_BASE_URL=https://boost-dev-3fabf.web.app
